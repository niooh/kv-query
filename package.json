{
  "name": "kv_query_app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build_index.js && vite build && node scripts/postbuild.js",
    "build:index": "node scripts/build_index.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.46.2",
    "vite": "^8.0.16"
  }
}